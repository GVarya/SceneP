<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="#000000"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/welcome"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:text="Добро пожаловать в (название придумаю потом) выберите способ работы"
        android:textAlignment="center"
        android:textColor="#ffba00"
        android:textSize="30sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.056" />

    <Button
        android:id="@+id/to_scenes_button"
        android:layout_width="170dp"
        android:layout_height="75dp"
        android:layout_below="@id/welcome"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:background="#ffba00"
        android:text="Сцены"
        app:layout_constraintBottom_toTopOf="@+id/to_spotlight_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/welcome"
        app:layout_constraintVertical_bias="0.833" />

    <Button
        android:id="@+id/to_spotlight_button"
        android:layout_width="170dp"
        android:layout_height="75dp"
        android:layout_below="@id/to_scenes_button"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="304dp"
        android:background="#ffba00"
        android:text="прожектора"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>